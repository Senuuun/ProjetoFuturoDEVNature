import React from 'react';
import ReactDOM from 'react-dom/client'; // Atualizado
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root')); // Atualizado
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);